'use client'

import React, { useEffect } from 'react'
import ProjectPage from '@/components/ProjectPage'

export default function PageDesignSingle({ data }) {
  return (
    <>
      <ProjectPage data={data} />
    </>
  )
}
